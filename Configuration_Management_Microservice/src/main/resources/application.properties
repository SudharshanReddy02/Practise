spring.application.name=configuration-management-service
spring.datasource.url=jdbc:mysql://localhost:3306/network
spring.datasource.username=root
spring.datasource.password=Nani@143
spring.security.user.name=wipro
spring.security.user.password=team2
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.jpa.hibernate.ddl-auto=none



# Hibernate settings (JPA)
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect



# Server Configuration
server.port=8080
#server.servlet.context-path=/nms     # The context path for the application

# Logging Configuration
#logging.level.root=INFO              # Set the logging level (INFO, DEBUG, ERROR, etc.)
#logging.file.name=logs/app.log       # File to write logs to (if applicable)
#logging.pattern.file=%d{yyyy-MM-dd HH:mm:ss} - %msg%n  # Log pattern

#log.file.path=c:\\Users\Administrator\gns3RouterLogs\R1_router_Logs.txt

# File Storage Configuration (for backups)
#backup.path=c:\\Users\Administrator\gns3ConfigFiles\R1_router_backup.txt   # Path where backup files will be stored
#config.file.path=c:\\Users\Administrator\gns3RouterConfigFiles\R1_router.txt

# If you are using a database (for example, for tracking configurations)
# Uncomment and configure if needed
# spring.datasource.url=jdbc:mysql://localhost:3306/your_db
# spring.datasource.username=your_username
# spring.datasource.password=your_password
# spring.jpa.hibernate.ddl-auto=update   # Use 'update', 'create', etc. based on your needs

# Other custom properties can be added here
# e.g. router IP and port could also be configured as properties
#router.ip=172.20.0.2                    # Router's external IP address
#router.port=23                           # Port used for connection

